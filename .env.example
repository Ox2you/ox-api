# Server
PORT=3000
CORS_ORIGINS=http://localhost:19006,http://localhost:5173

# Database (Supabase Transaction Pooler)
DATABASE_URL=postgresql://postgres.wimeikijgvycegyjotfd:Thelastofbugs%402025@aws-1-us-east-2.pooler.supabase.com:6543/postgres?sslmode=require

# SSL knobs (opcionais)
# PG_STRICT_SSL=1         # força verificação estrita (use com CA válida)
# PG_SKIP_TLS_VERIFY=1    # ignora verificação (normalmente desnecessário)
# PG_SSL_CA_PATH=         # caminho para CA (ex.: /etc/ssl/certs/ca.pem)
# PG_SSL_CA=              # conteúdo PEM da CA (inline)

# Cache / rate-limit
HEATMAP_CACHE_TTL=300
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120
HEATMAP_RATE_LIMIT_WINDOW_MS=60000
HEATMAP_RATE_LIMIT_MAX=60

# Redis (optional). If empty, in-memory cache is used.
REDIS_URL=
